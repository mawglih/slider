import React from 'react';
import Slider from 'components/Slider';

const Home = () => {
  return (
    <div>
      <Slider
        sliderClass="red"
        sliderName="Age range"
      />
    </div>
  )
}

export default Home;
